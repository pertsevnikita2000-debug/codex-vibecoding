# codex-vibecoding

Мини-проект с готовой концепцией «современной стильной фотосессии».

## Как открыть

Просто открой `index.html` в браузере или запусти локальный сервер:

```bash
python3 -m http.server 8000
```

И перейди на `http://localhost:8000`.
